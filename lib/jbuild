(jbuild_version 1)

(library
 ((name            hnsw)
  (public_name     hnsw)
  (modules (hnsw_algo hnsw ohnsw))
  (libraries       (core_kernel lacaml stdio))
  (inline_tests)
  (preprocess (pps (ppx_jane)))))

